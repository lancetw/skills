[project]
name = "bible-buddy"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "trafilatura",
    "patchright",
    "pygments>=2.18,<2.20",
    "text-fabric>=13.1.0",
    "pymupdf4llm>=1.27.2.2",
]

[tool.uv]
# After `uv sync`, run: uv run patchright install chromium
