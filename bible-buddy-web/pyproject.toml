[project]
name = "bible-buddy-web"
version = "1.0.0"
description = "Local web Bible reader. The Agent SDK runs the bible-buddy skill; annotations land on the passage."
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "claude-agent-sdk",
]
