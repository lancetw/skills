[project]
name = "bible-buddy-web-proto"
version = "0.0.1"
description = "PROTOTYPE — throwaway. Agent SDK runs the bible-buddy skill; annotations land on the passage."
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "claude-agent-sdk",
]
